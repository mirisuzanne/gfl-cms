<form
  :name="name"
  :action="action"
  method="POST"
>
  <slot>
  </slot>

  <form-field
    @label="Current URL"
    name="url"
    :@value="$data.page.url"
    hidden></form-field>
  <div class="form-actions">
    <button type="submit" @html="submit || submit"></button>
  </div>
</form>
