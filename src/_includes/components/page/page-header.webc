<hero-image webc:if="hero" id="hero" :@hero="hero"></hero-image>

<header id="banner">
  <p webc:if="feature" class="pretitle">Presenting:</p>
  <h1 @html="mdInline(title)"></h1>
  <p webc:if="subtitle" class="subtitle" @html="mdInline(subtitle)"></p>
</header>
